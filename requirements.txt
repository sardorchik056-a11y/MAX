aiogram>=3.4,<4
aiohttp>=3.9

# ⚠️ Это только зависимости, нужные main.py/subscription.py для запуска через вебхук.
# Добавьте сюда всё остальное, что использует остальной код бота — например, если
# payments.py ходит в CryptoBot/xRocket через httpx/requests, добавьте их пакеты тоже.
